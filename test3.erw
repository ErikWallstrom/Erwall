type Player: struct(
	mut x: Int32,
	mut y: Int32
);

func test: (let p_ref: &Player)
{
	p_ref&.x = 3;
	p_ref&.y = 97;
}

func main: () -> Int32
{

	let p: Player;
	p.x = 12;
	p.y = 44;

	test(&p);
}
