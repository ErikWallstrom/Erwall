type Player: struct(
	mut x: Int32,
	mut y: Int32,
	let update: func(Player) -> Player
);

func player_update: (let self: Player) -> Player
{
	return self;
}

func test: (let x: Int32) -> Int32
{
	while(true)
	{
		return x;
	}
}

func main: () -> Int32
{
#[
	let x: [[]Char]Int32;
	x["hello"] = 12;

	let num: Int64 = Int64(44);
	let numbers: [5]Int64 = [5]Int64(33, 44, 55, 66, 77);
	let player: Player = Player(x: 12, y: 44, update: test);
	let day: Weekday = Weekday.monday;
	let result: Result = Result.None();
#]
	return 0;
}

