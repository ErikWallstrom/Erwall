type Player: struct(
	mut x: Int32,
	mut y: Int32
);

type Players: struct(let players: []Player);

func test: (let x: Int32) 
{
	
}

func main: () -> Int32
{
	let po: [3]Int32;
	po[0] = 12;

	let p: Players;
	let p_ref: &Players = &p;

	test(p_ref&.players[2].y);
	return 0;
}

