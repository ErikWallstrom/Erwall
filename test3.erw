type Player: struct();
type Union: union(Int32, Player, Float32);
type Weekdays: enum(monday, tuesday = 4, wednesday, friday);

func player_ctor: (let self: &Player) -> &Player
{
	return self;
}

func test: ()
{
	
}

func main: () -> Int32
{
	let p: Player;
	player_ctor(&p);

	let c: Char = 'M';

	let f: func(Int32, &Float32) -> Float32 = test;
	return 0;
}

