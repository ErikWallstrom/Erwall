type Int: Int32;

func loop: (let times: UInt32)
{ 
	@printf("Loop iteration: %u\n", times);
	if(times > UInt32(0))
	{ 
		loop(times - UInt32(1));
	}
}

func main: () -> Int32
{ 
	loop(UInt32(13) - UInt32(2));
	return 0;
}
