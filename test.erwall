type Int: Int32;

func add: (let x: Int32, let y: Int32) -> Int32
{ 
	return x + y;
}

func main: () -> Int32
{ 
	if(10 < add(1, 2))
	{ 
		@puts("Wtf???");
	}
	elseif(11 == add(1, 2))
	{ 
		@puts("Wtf???");
	}
	else
	{ 
		@puts("Indeed");
	}

	return 0;
}
