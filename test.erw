type Int: Int32;

func test: (let x: Int) -> Int
{
	return cast(Int, cast(Float32, x) + 12.0 + 3.0);
}

func main: () -> Int32
{ 
	type Lol: Int32;
	func tata: () -> Lol
	{
		let x: Int32 = 12;
		return cast(Lol, x + 13);
	}

	let y: Lol = tata();
	test(cast(Int, 12));
	return 0;
}

