func fib: (let n: Int32) -> Int32
{
	if(n == 0)
	{
		return 0;
	}
	elseif(n == 1 || n == 2)
	{
		return 1;
	}
	else
	{
		return fib(n - 1) + fib(n - 2);
	}
}

func main: () -> Int32
{ 
	let hello: Int32 = 12;
# fib(x);
	func test: ()
	{
		type Int: Int32;
		func testa: (let yaaslkdj: Int)
		{
# @puts(yaaslkdj);
		}

		testa(cast(Int, 3));
	}

	test();
	if(hello < 3)
	{
	}

	@printf("%i\n", fib(30));
	return 0;
}

