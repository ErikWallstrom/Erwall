type Test: Int32;

func test: (let x: Int32) -> Bool
{
	@printf("%i\n", x);
	return false;
}

func main: () -> Int32
{
	mut x: Test = cast(Test, 12);
	x += cast(Test, 24);
	test(cast(Int32, x));
	return 0;
}

